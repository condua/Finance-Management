import { StyleSheet, Text, View } from "react-native";
import React from "react";

type Props = {};

const modalImage = (url: string) => {
  return (
    <View>
      <Text>modalImage</Text>
    </View>
  );
};

export default modalImage;

const styles = StyleSheet.create({});
